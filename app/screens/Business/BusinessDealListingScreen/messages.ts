/*
 * BusinessDealListingScreen Messages
 *
 * This contains all the text for the BusinessDealListingScreen.
 *
 */

import { defineMessages } from 'react-intl';
const scope = 'app.screens.Business.BusinessDealListingScreen';

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'All Deals',
  },
});
