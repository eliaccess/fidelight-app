/*
 * BusinessDealListingScreen Messages
 *
 * This contains all the text for the BusinessDealListingScreen component.
 *
 */

import { defineMessages } from 'react-intl';
const scope = 'app.screens.BusinessDealListingScreen';
export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'All Deals',
  },
});
