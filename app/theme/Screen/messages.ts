/*
 * Screen Messages
 *
 * This contains all the text for the Screen component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.screens.Screen';

export default defineMessages({
  backLabel: {
    id: `${scope}.backLabel`,
    defaultMessage: 'Back',
  },
});
