/*
 * modal Messages
 *
 * This contains all the text for the modal component.
 *
 */

import { defineMessages } from 'react-intl';
const scope = 'app.theme.modal';
export default defineMessages({
  close: {
    id: `${scope}.close`,
    defaultMessage: 'Close',
  },
});
