/*
 * DealDetailScreen Messages
 *
 * This contains all the text for the DealDetail screen.
 *
 */

import { defineMessages } from 'react-intl';

const scope = 'app.screens.DealDetailScreen';

export default defineMessages({
  offerDetailLabel: {
    id: `${scope}.offerDetailLabel`,
    defaultMessage: 'Offer Detail',
  },
});
