/*
 * DealListingScreen Messages
 *
 * This contains all the text for the DealListingScreen component.
 *
 */

import { defineMessages } from 'react-intl';
const scope = 'app.screens.DealListingScreen';
export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'Hottest Deals',
  },
});
