/*
 * ExploreScreen HottestDeals Messages
 *
 * This contains all the text for the ExploreScreen HottestDeals component.
 *
 */

import { defineMessages } from 'react-intl';

const scope = 'app.screens.ExploreScreen.HottestDeals';

export default defineMessages({
  dealsHeading: {
    id: `${scope}.dealsHeading`,
    defaultMessage: 'Hottest Deals',
  },
  seeAllLabel: {
    id: `${scope}.seeAllLabel`,
    defaultMessage: 'See All',
  },
});
