/*
 * Separator Messages
 *
 * This contains all the text for the Separator component.
 *
 */

import { defineMessages } from 'react-intl';
const scope = 'app.screen.Separator';
export default defineMessages({
  orLabel: {
    id: `${scope}.orLabel`,
    defaultMessage: 'OR',
  },
});
